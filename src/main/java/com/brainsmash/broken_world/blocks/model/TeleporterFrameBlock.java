package com.brainsmash.broken_world.blocks.model;

import net.minecraft.block.Block;

public class TeleporterFrameBlock extends Block {
    public TeleporterFrameBlock(Settings settings) {
        super(settings);
    }
}
