package com.brainsmash.broken_world.blocks.magical;

import net.minecraft.block.Block;

public class MagicCircle extends Block {
    public MagicCircle(Settings settings) {
        super(settings);
    }
}
